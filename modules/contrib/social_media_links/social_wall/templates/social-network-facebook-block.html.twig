<ul class="social-networks facebook">
  <p class="social-networks-name">Facebook</p>
  {% for post in elements %}
    <li class="social-networks-items">
      <div class="social-networks-details">
        <p class="social-networks-date">{{ post.creation_timestamp|format_date('short') }}</p>
        <p class="social-networks-post">{{ post.body_text }}</p>
        <a class="see-more" href="{{ post.post_url }}" target="_blank" title="{{ 'See post on @sn (new window)'|t({ '@sn': 'Facebook' }) }}">
          {{ 'See post on Facebook'|t }}
        </a>
      </div>
    </li>
  {% endfor %}
</ul>
